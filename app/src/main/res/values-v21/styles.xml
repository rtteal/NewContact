<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.SimpleContacts" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:colorPrimary">@color/material_blue_500</item>
        <item name="android:colorPrimaryDark">@color/material_blue_700</item>
        <item name="android:colorAccent">@color/material_green_200</item>
        <item name="android:textColorPrimary">@android:color/black</item>
        <item name="android:windowBackground">@android:color/white</item>
        <item name="android:navigationBarColor">@color/material_blue_500</item>
        <item name="android:textSize">20sp</item>
        <item name="android:background">?android:attr/selectableItemBackground</item>
        <item name="android:colorControlHighlight">@android:color/darker_gray</item>

        <!-- enable window content transitions -->
        <item name="android:windowContentTransitions">true</item>

        <!-- specify enter and exit transitions -->
        <item name="android:windowEnterTransition">@transition/slide_up</item>
        <item name="android:windowExitTransition">@transition/slide_down</item>

        <!-- specify shared element transitions -->
        <item name="android:windowSharedElementEnterTransition">@transition/slide_up</item>
        <item name="android:windowSharedElementExitTransition">@transition/slide_down</item>

        <!-- begin add of stuff from Android L Preview.... -->
        <!-- Main theme action bar-->
        <item name="android:actionBarStyle">@style/MainActionBar</item>
        <item name="android:actionBarTheme">@style/MainActionBarThemeOverlay</item>

        <!-- Flag indicating whether this Window's transition should overlap with
                     the exiting transition of the calling Activity. -->
        <item name="android:windowAllowEnterTransitionOverlap">true</item>

        <!--Flag indicating whether this Window's transition should overlap with
             the exiting transition of the called Activity when the called Activity
             finishes. -->
        <item name="android:windowAllowReturnTransitionOverlap">true</item>

        <!-- Flag indicating whether this window's Action Bar should overlay -->
        <item name="android:windowActionBarOverlay">true</item>

        <!-- Flag indicating whether this window's Action Bar content should overlay -->
        <item name="android:windowContentOverlay">@null</item>

        <item name="android:windowSharedElementsUseOverlay">false</item>
    </style>

    <style name="MainActionBar" parent="Widget.AppCompat.Toolbar">
        <item name="android:background">?android:colorPrimary</item>
        <item name="android:displayOptions">showTitle|homeAsUp</item>
       <!-- <item name="android:homeAsUpIndicator">@drawable/ic_ab_drawer</item> -->
        <item name="android:titleTextStyle">@style/MainThemeActionBarTitleTextStyle</item>
    </style>


    <style name="DetailActionBar" parent="Widget.AppCompat.Toolbar">
        <item name="android:background">@null</item>
        <item name="android:displayOptions">homeAsUp</item>
    </style>


    <style name="MainActionBarThemeOverlay" parent="">
        <item name="android:colorControlNormal">?actionBarIconColor</item>
        <item name="android:colorControlHighlight">@color/material_blue_500</item>
    </style>


    <style name="MainThemeActionBarTitleTextStyle" parent="@android:style/TextAppearance.Material.Widget.ActionBar.Title">
        <item name="android:textColor">#fff</item>
        <item name="android:fontFamily">sans-serif-light</item>
    </style>


    <declare-styleable name="BaseAppTheme">
        <attr name="actionBarIconColor" format="color" />
    </declare-styleable>


    <style name="DetailActivityTheme" parent="Theme.SimpleContacts">
        <item name="android:actionBarStyle">@style/DetailActionBar</item>
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:windowTranslucentNavigation">true</item>
        <item name="android:windowIsTranslucent">true</item>
    </style>

</resources>