<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.simplecontacts" >

    <uses-sdk
        android:maxSdkVersion="21"
        android:minSdkVersion="16"
        android:targetSdkVersion="21" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:theme="@style/SimpleContactsTheme"
        android:label="Contact">
        <activity
            android:name="com.contact.MainActivity"
            android:theme="@style/SimpleContactsTheme.Main" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.contact.ContactActivity"
            android:theme="@style/SimpleContactsTheme.DetailActivityTheme" >
        </activity>
    </application>

</manifest>
